<section
	class="layered-card z-10 relative overflow-hidden"
	id="inicio"
	aria-labelledby="hero-title"
>
	<!-- Atmospheric background -->
	<div class="absolute inset-0 overflow-hidden pointer-events-none">
		<div
			class="hero-bg-circle absolute -top-32 -right-32 w-[500px] h-[500px] rounded-full opacity-20"
			style="background: radial-gradient(circle, rgba(0,93,170,0.12) 0%, transparent 70%);"
		>
		</div>
		<div
			class="hero-bg-circle absolute top-1/4 -left-40 w-80 h-80 rounded-full opacity-15"
			style="background: radial-gradient(circle, rgba(232,115,74,0.08) 0%, transparent 70%); animation-delay: -5s;"
		>
		</div>
		<div
			class="hero-bg-circle absolute bottom-20 right-1/4 w-64 h-64 rounded-full opacity-10"
			style="background: radial-gradient(circle, rgba(0,93,170,0.1) 0%, transparent 70%); animation-delay: -8s;"
		>
		</div>
		<!-- Floating particles -->
		<div
			class="hero-bg-element absolute top-20 right-20 w-3 h-3 rounded-full bg-primary/30"
		>
		</div>
		<div
			class="hero-bg-element absolute bottom-32 left-10 w-2 h-2 rounded-full bg-accent/40"
		>
		</div>
		<div
			class="hero-bg-element absolute top-1/2 right-1/4 w-4 h-4 rounded-full bg-primary/20"
		>
		</div>
		<div
			class="hero-bg-element absolute top-1/3 left-1/3 w-1.5 h-1.5 rounded-full bg-accent/30"
		>
		</div>
		<div
			class="hero-floating absolute top-1/4 left-1/4 w-2 h-2 rounded-full bg-primary/50"
			style="animation-delay: -1s;"
		>
		</div>
		<div
			class="hero-floating absolute bottom-1/4 right-10 w-3 h-3 rounded-full bg-primary/30"
			style="animation-delay: -2s;"
		>
		</div>
		<!-- Decorative line -->
		<div
			class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-px h-48 bg-gradient-to-b from-transparent via-primary/10 to-transparent"
		>
		</div>
	</div>

	<div
		class="card-content items-center text-center p-6 md:p-12 lg:p-16 central-glow relative z-10"
	>
		<div class="space-y-6 md:space-y-10 max-w-5xl mx-auto">
			<!-- Eyebrow -->
			<div class="reveal-slide">
				<span
					class="inline-block text-[10px] md:text-[11px] font-bold tracking-[0.4em] uppercase text-ink-faint mb-2"
					>Núcleo Universitario Rafael Rangel</span
				>
				<div class="w-12 h-px bg-primary/30 mx-auto mt-4"></div>
			</div>

			<!-- Main heading -->
			<h1
				id="hero-title"
				class="text-4xl sm:text-5xl md:text-7xl lg:text-[9rem] font-display font-bold tracking-tight leading-[0.95] md:leading-[0.85] text-ink text-wrap balance"
			>
				Comunicación<br />
				<span class="text-primary font-bold">Social</span>
			</h1>

			<!-- Subtitle -->
			<p
				class="text-base md:text-lg lg:text-xl font-normal text-ink-muted max-w-2xl mx-auto leading-relaxed"
			>
				Explorando las nuevas fronteras del lenguaje y la narrativa
				digital en el Núcleo Universitario Rafael Rangel.
			</p>

			<!-- CTA -->
			<div class="hero-cta pt-4">
				<a
					href="#noticias"
					class="btn-magnetic inline-flex items-center justify-center gap-2 bg-primary text-white px-8 py-4 rounded-full text-sm font-bold tracking-wider uppercase hover:shadow-xl hover:shadow-primary/30 focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 w-full md:w-auto"
				>
					<span>Descubre más</span>
					<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
						<path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>
					</svg>
				</a>
			</div>
		</div>
	</div>
</section>

<style>
	.hero-bg-circle {
		will-change: transform, opacity;
	}

	.hero-bg-element {
		will-change: transform, opacity;
	}

	.hero-floating {
		animation: float 4s ease-in-out infinite;
	}

	@keyframes float {
		0%,
		100% {
			transform: translateY(0);
		}
		50% {
			transform: translateY(-15px);
		}
	}
</style>
