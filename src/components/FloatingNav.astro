<nav class="reveal fixed left-4 md:left-8 top-1/2 -translate-y-1/2 z-[110] flex-col gap-1 hidden md:flex" aria-label="Navegación principal" id="side-nav">
	<!-- Connecting line -->
	<div class="absolute left-1/2 -translate-x-1/2 top-2 bottom-2 w-px bg-black/[0.04]" aria-hidden="true"></div>
	
	<!-- Active indicator dot -->
	<div class="active-dot absolute -left-1 w-1 h-1 rounded-full bg-primary transition-all duration-500 ease-out" aria-hidden="true"></div>
	
	<a class="nav-pill p-3 rounded-full flex items-center justify-center group relative" href="#inicio" data-section="inicio" title="Inicio" aria-label="Ir a inicio">
		<span class="nav-tooltip absolute left-full ml-3 px-3 py-1.5 bg-ink text-white text-[10px] font-bold tracking-wider uppercase rounded-lg opacity-0 -translate-x-2 transition-all duration-300 pointer-events-none whitespace-nowrap group-hover:opacity-100 group-hover:translate-x-0">Inicio</span>
		<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>
	</a>
	<a class="nav-pill p-3 rounded-full flex items-center justify-center group relative" href="#noticias" data-section="noticias" title="Noticias" aria-label="Ir a noticias">
		<span class="nav-tooltip absolute left-full ml-3 px-3 py-1.5 bg-ink text-white text-[10px] font-bold tracking-wider uppercase rounded-lg opacity-0 -translate-x-2 transition-all duration-300 pointer-events-none whitespace-nowrap group-hover:opacity-100 group-hover:translate-x-0">Noticias</span>
		<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7Z"/><path d="M14 2v4a2 2 0 0 0 2 2h4"/><path d="M10 9H8"/><path d="M16 13H8"/><path d="M16 17H8"/></svg>
	</a>
	<a class="nav-pill p-3 rounded-full flex items-center justify-center group relative" href="#video" data-section="video" title="Experiencia" aria-label="Ir a experiencia">
		<span class="nav-tooltip absolute left-full ml-3 px-3 py-1.5 bg-ink text-white text-[10px] font-bold tracking-wider uppercase rounded-lg opacity-0 -translate-x-2 transition-all duration-300 pointer-events-none whitespace-nowrap group-hover:opacity-100 group-hover:translate-x-0">Experiencia</span>
		<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polygon points="6 3 20 12 6 21 6 3"/></svg>
	</a>
	<div class="w-px h-6 bg-black/[0.05] mx-auto my-1" aria-hidden="true"></div>
	<a class="nav-pill p-3 rounded-full flex items-center justify-center group relative" href="#accesos" data-section="accesos" title="Accesos" aria-label="Ir a accesos rápidos">
		<span class="nav-tooltip absolute left-full ml-3 px-3 py-1.5 bg-ink text-white text-[10px] font-bold tracking-wider uppercase rounded-lg opacity-0 -translate-x-2 transition-all duration-300 pointer-events-none whitespace-nowrap group-hover:opacity-100 group-hover:translate-x-0">Accesos</span>
		<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect width="7" height="7" x="3" y="3" rx="1"/><rect width="7" height="7" x="14" y="3" rx="1"/><rect width="7" height="7" x="14" y="14" rx="1"/><rect width="7" height="7" x="3" y="14" rx="1"/></svg>
	</a>
</nav>

<style>
	.nav-pill svg {
		color: #005daa;
		transition: stroke-width 0.3s ease;
	}
	
	.nav-pill.is-active svg {
		stroke-width: 2;
	}
	
	.nav-pill.is-active {
		background: rgba(0, 93, 170, 0.08);
		border-color: rgba(0, 93, 170, 0.15);
	}
	
	.active-dot {
		top: 50%;
		transform: translateY(-50%);
		opacity: 0;
		transition: top 0.5s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.3s ease;
	}
	
	.active-dot.visible {
		opacity: 1;
	}
</style>

<script>
	// Scroll-based active section detection
	const sections = ['inicio', 'noticias', 'video', 'accesos'];
	const navLinks = document.querySelectorAll('#side-nav a[data-section]');
	const activeDot = document.querySelector('#side-nav .active-dot');
	
	function updateActiveNav() {
		let currentSection = '';
		
		sections.forEach(id => {
			const el = document.getElementById(id);
			if (el) {
				const rect = el.getBoundingClientRect();
				const isVisible = rect.top <= window.innerHeight * 0.5 && rect.bottom >= window.innerHeight * 0.3;
				if (isVisible) {
					currentSection = id;
				}
			}
		});
		
		navLinks.forEach(link => {
			const isActive = link.getAttribute('data-section') === currentSection;
			link.classList.toggle('is-active', isActive);
		});
		
		// Move active dot to active link
		const activeLink = document.querySelector('#side-nav a.is-active');
		if (activeLink && activeDot) {
			const navRect = document.getElementById('side-nav')?.getBoundingClientRect();
			const linkRect = activeLink.getBoundingClientRect();
			if (navRect && linkRect) {
				const relativeTop = linkRect.top - navRect.top + linkRect.height / 2;
				activeDot.style.top = `${relativeTop}px`;
				activeDot.classList.add('visible');
			}
		} else if (activeDot) {
			activeDot.classList.remove('visible');
		}
	}
	
	// Throttled scroll listener
	let ticking = false;
	window.addEventListener('scroll', () => {
		if (!ticking) {
			requestAnimationFrame(() => {
				updateActiveNav();
				ticking = false;
			});
			ticking = true;
		}
	}, { passive: true });
	
	// Initial check
	updateActiveNav();
</script>
